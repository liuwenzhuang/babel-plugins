var code = 'abc';
var arr = [1, 2, 3];
var obj = {
    name: 'jane',
    age: 12
};

obj.name = 'maria';
obj['age'] = 15;

obj.name;
obj['age'];

mori.assign(obj, 'gender', 'male');

2 | double | square | bala
'text' | jane
'bala' | double

const doubleAndSquare = x => x | double | square
const fooAndBar = x => x & foo | bar

import Toast, { Button as Button1, Table } from 'antd';
import Text, { Comment } from './Text';